---
import Layout from "../../layouts/Layout.astro";

let data = "All data, other than this endpoint, returns JSON.\n";
data += "\nOne-offs:";
data += "\n  GET /api/v1/headline.json            Headline text.";
data += "\n  GET /api/v1/meetingschedule.json     Schedule of upcoming and current meetings.\n";
data += "\nCitizens:";
data += "\n  GET /api/v1/citizens/royalty.json     Citizens part of the royalty.";
data += "\n  GET /api/v1/citizens/dukes.json       Citizens which are dukes.";
data += "\n  GET /api/v1/citizens/general.json     General citizens.";
data += "\n  GET /api/v1/citizens/all.json         All of the citizens together.";
---

<Layout title="Stoako | API">
    <pre><code>{data}</code></pre>
</Layout>